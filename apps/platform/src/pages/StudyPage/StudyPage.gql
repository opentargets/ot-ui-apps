query StudyPageQuery($studyId: String!) {
  gwasStudy(studyId: $studyId) {
    studyId
    projectId
    traitFromSource
    backgroundTraits
    target {
      id
    }
    diseases {
      id
    }
  }
}
