query StudyPageQuery($studyId: String!) {
  study(studyId: $studyId) {
    id
    studyType
    projectId
    backgroundTraits {
      id
      name
    }
    target {
      id
    }
    diseases {
      id
      name
    }
  }
}
