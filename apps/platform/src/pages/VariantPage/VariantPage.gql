query VariantPageQuery($variantId: String!) {
  variant(variantId: $variantId) {
    id
    dbXrefs {
      id
      source
    }
    chromosome
    position
    referenceAllele
    alternateAllele
  }
}
