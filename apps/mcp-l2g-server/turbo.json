{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "passThroughEnv": ["ANTHROPIC_API_KEY"]
    },
    "build": {
      "outputs": ["dist/widgets/**"]
    }
  }
}
