query TargetTooltipQuery($ensgId: String!) {
  target(ensemblId: $ensgId) {
    id
    name: approvedName
    description: functionDescriptions
    genomicLocation {
      chromosome
      start
    }
  }
}
