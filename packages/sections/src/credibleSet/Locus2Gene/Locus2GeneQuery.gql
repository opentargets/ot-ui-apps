query VariantsQuery($studyLocusIds: [String!]!) {
  credibleSets(studyLocusIds: $studyLocusIds) {
    studyLocusId
    l2Gpredictions {
      target {
        id
        approvedSymbol
      }
      score
    }
    proteinIds
  }
}
