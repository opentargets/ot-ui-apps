query SysBioQuery($ensemblId: String!, $efoId: String!) {
  disease(efoId: $efoId) {
    id
    evidences(
      ensemblIds: [$ensemblId]
      enableIndirect: true
      datasourceIds: ["sysbio"]
    ) {
      rows {
        disease {
          id
          name
        }
        target {
          id
          approvedSymbol
        }
        studyOverview
        literature
        pathways {
          name
        }
      }
    }
  }
}
