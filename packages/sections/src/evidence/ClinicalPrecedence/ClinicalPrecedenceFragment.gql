fragment ClinicalPrecedenceSummaryFragment on Disease {
  clinical_precedence: evidences(
    ensemblIds: [$ensgId]
    enableIndirect: true
    datasourceIds: ["clinical_precedence"]
    size: 1
  ) {
    count
  }
}