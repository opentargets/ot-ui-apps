query ClinicalPrecedence($ensemblId: String!, $efoId: String!, $size: Int!, $cursor: String) {
  disease(efoId: $efoId) {
    id
    clinical_precedence: evidences(
      ensemblIds: [$ensemblId]
      enableIndirect: true
      datasourceIds: ["clinical_precedence"]
      size: $size
      cursor: $cursor
    ) {
      cursor
      count
      rows {
        disease {
          id
          name
        }
        target {
          id
          approvedSymbol
        }
        drug {
          id
          name
          mechanismsOfAction {
            rows {
              targets {
                id
                approvedSymbol
              }
            }
          }
        }
        directionOnTarget
        directionOnTrait
        targetFromSourceId
        clinicalStage
        clinicalReportId
        studyStartDate

      }
    }
  }
}