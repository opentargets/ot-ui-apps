query InSilicoPredictorsQuery($variantId: String!) {
  variant(variantId: $variantId) {
    variantId
    inSilicoPredictors {
      method
      assessment
      score
      assessmentFlag
    }
    referenceAllele
    alternateAllele
  }
}