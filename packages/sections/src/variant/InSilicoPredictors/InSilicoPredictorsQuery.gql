query InSilicoPredictorsQuery($variantId: String!) {
  variant(variantId: $variantId) {
    id
    inSilicoPredictors {
      method
      assessment
      score
      assessmentFlag
      normalisedScore
    }
    referenceAllele
    alternateAllele
  }
}
