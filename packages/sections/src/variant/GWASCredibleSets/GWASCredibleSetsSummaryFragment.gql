query GWASCredibleSetsQuery($variantId: String!) {
  variant(variantId: $variantId) {
    variantId
    referenceAllele
    alternateAllele
    credibleSets(studyTypes: [gwas]) {
      variant {
        variantId
      }
      study {
        
      }
      


    }
  }
}